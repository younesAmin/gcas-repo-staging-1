version: 0.1
component: build
timeoutInSeconds: 1000
shell: bash

steps:
  - type: Command
    name: "compress the repo"
    command: |
      zip repo.zip ./
outputArtifacts:
  - name: artifact
    type: BINARY
    location: ${OCI_WORKSPACE_DIR}/repo/repo.zip
